/**
 * Simple TypeScript code sample for testing basic transformation
 */
export function greetUser(name: string): string {
	return `Hello, ${name}!`
}

export const CONFIG = {
	version: '1.0.0',
	debug: false,
}

// Simple variable declaration for testing
export const currentDate = new Date()
